---
tags: [web, network, osi, l4, tcp, udp, port, segment]
status: done
category: "웹&네트워크(Web&Network)"
aliases: ["Transport Layer", "전송 계층", "트랜스포트 계층", "L4", "TCP UDP 포트", "세그먼트"]
---

# 전송 계층 (Transport Layer, L4)

네트워크 계층이 패킷을 목적지 호스트까지 보내면, 전송 계층은 그 호스트 안의 **어느 애플리케이션으로 보낼지**를 정하고 **얼마나 믿고 보낼지**(신뢰성과 속도)를 결정한다. 전체 지도는 [[OSI-7-Layer]], 바로 아래 L3는 [[Network-Layer]]. 한 줄 요약: **L3는 호스트까지, L4는 그 안의 프로그램까지.**

## L4가 푸는 문제 — 애플리케이션 구분

L3의 IP 주소는 호스트(컴퓨터 한 대)까지만 가리킨다([[Network-Layer]]). 한 노트북에서 웹 브라우저, 메신저, 게임이 동시에 통신할 때 들어온 데이터가 어느 프로그램 몫인지는 IP만으로 못 가린다. L4가 다루는 문제는 셋이다.

1. **멀티플렉싱**: 데이터를 어느 애플리케이션에 줄지 구분(포트).
2. **순서**: 데이터가 순서대로 도착했는지 확인.
3. **유실 대응**: 중간에 사라진 데이터를 어떻게 처리할지 결정.

대표 프로토콜은 TCP와 UDP, 데이터 단위는 세그먼트다.

## 세그먼트와 캡슐화

L4의 데이터 단위 세그먼트는 L3의 패킷 안에 담겨 이동한다. 계층마다 단위가 다르고 서로 감싼다.

프레임(L2) ⊃ 패킷(L3) ⊃ 세그먼트(L4) ⊃ 실제 데이터. 택배 상자 안에 작은 상자가 있고 그 안에 물건이 든 구조다.

## 포트 — 호스트 안의 방 번호

IP가 건물 주소라면 포트는 그 건물 안의 방 번호다. 같은 IP로 도착한 데이터가 웹 서버용인지 SSH용인지 DB용인지를 구분하는 논리적 번호이며, TCP와 UDP 각각 0번부터 65535번까지 쓴다.

| 포트 | 용도 |
|---|---|
| 22 | SSH |
| 53 | DNS |
| 80 | HTTP |
| 443 | HTTPS |
| 3306 | MySQL |

### 웰노운 포트 vs 임시 포트

- **웰노운 포트(well-known)**: 약속처럼 고정된 포트(HTTP 80, HTTPS 443, SSH 22). 서버가 여기서 대기한다.
- **임시 포트(ephemeral port)**: 클라이언트가 통신할 때마다 남는 번호를 임시로 잡는다. 브라우저가 웹 서버에 붙을 때 목적지 포트는 443이지만 출발지(클라이언트) 포트는 매번 달라진다.

**서버가 고정 포트를 쓰는 이유**: 불특정 다수 클라이언트가 어디로 접속할지 알아야 하기 때문이다. `https://example.com`에 포트를 안 붙여도 되는 건 브라우저가 HTTPS 기본 포트 443을 자동으로 시도해서다. 포트는 설정으로 바꿀 수 있지만, 기본 약속과 다르면 주소에 포트를 직접 명시해야 한다.

## TCP — 신뢰성 우선

TCP(Transmission Control Protocol)는 전송을 통제하고 확인하는 데 초점이 있다. 데이터를 보낸 뒤 상대가 받았는지 확인하고, 응답이 없으면 유실로 보고 재전송한다. 그래서 **순서 보장, 유실 대응, 무결성 확인**이 필요한 통신(웹, HTTPS, 이메일, 파일 전송, SSH)에 맞는다.

대신 보낼 때마다 응답을 기다리므로 상대적으로 느리다. 확인 절차가 신뢰성을 만들지만 지연과 추가 트래픽을 낳는다. TCP는 데이터 전에 **연결을 먼저 수립**한다(3-way handshake) — 상세 절차와 종료(4-way), TIME_WAIT, RTT 비용은 [[TCP-Handshake]].

### TCP 세그먼트의 주요 필드

| 필드 | 역할 |
|---|---|
| 출발지/목적지 포트 | 어느 애플리케이션인지 (멀티플렉싱) |
| 시퀀스 번호 | 지금 보내는 데이터의 순서 번호 |
| ACK 번호 | 여기까지 받았으니 다음은 이 번호부터 (기대 번호) |
| 플래그 | SYN, ACK, FIN 등 세그먼트의 목적 |
| 윈도우 사이즈 | 한 번에 받을 수 있는 양 (흐름 제어) |
| 체크섬 | 오류 검출 |

시퀀스/ACK 번호로 양쪽이 순서와 수신 여부를 계속 맞춘다. 예를 들어 시퀀스 13을 받으면 다음은 14부터 달라고 ACK 14로 응답한다. 윈도우 사이즈는 받는 쪽이 감당할 양을 알려 송신 속도를 조절하는 흐름 제어에 쓰인다.

## UDP — 속도와 단순함 우선

UDP(User Datagram Protocol)는 연결을 미리 맺지 않고 데이터를 바로 보낸다. 수신 확인을 안 하고, 순서가 바뀌거나 일부가 사라져도 기본적으로 재전송하지 않는다. 그래서 TCP보다 빠르고 구조가 단순한 대신 신뢰성은 낮다.

세그먼트에 출발지/목적지 포트, 길이, 체크섬, 데이터 정도만 들어간다. 시퀀스/ACK/윈도우/제어 플래그를 관리하지 않아 헤더가 작고 처리가 가볍다. 한 조각 빠져도 전체 경험에 큰 지장이 없는 서비스(스트리밍, 음성 통화, 온라인 게임)에 맞는다. 영상에서 한 프레임 누락은 사용자가 거의 못 느낀다.

## TCP vs UDP

| | TCP | UDP |
|---|---|---|
| 연결 | 연결 지향 (3-way handshake) | 비연결 (바로 전송) |
| 신뢰성 | 확인, 재전송, 순서 보장 | 미보장 (유실/순서 변동 허용) |
| 헤더 | 큼 (seq, ack, window, flags) | 작음 (포트, 길이, 체크섬) |
| 속도 | 상대적으로 느림 | 빠름 |
| 용도 | 웹, HTTPS, 이메일, 파일, SSH | 스트리밍, 음성, 게임, DNS |

선택 기준은 단순하다. 데이터가 틀리거나 빠지면 안 되면(웹, 로그인, 다운로드, 원격 접속) TCP, 약간의 손실보다 실시간성이 중요하면(음성, 게임, 영상) UDP. TCP는 정확히 보내기, UDP는 빠르게 보내기에 가깝다.

## AWS와 L4

L4 지식은 AWS 네트워킹의 기본기다. 로드 밸런서가 동작하는 계층이 곧 다룰 수 있는 정보를 정한다. [[ELB]]

- **Network Load Balancer(NLB)**: L4에서 동작 → TCP/UDP와 포트 기준으로 트래픽 처리. 초고성능, TCP/UDP 패스스루.
- **Application Load Balancer(ALB)**: L7에서 동작 → HTTP 경로, 헤더, 호스트 같은 애플리케이션 정보로 라우팅.

VPC, 보안 그룹(SG), NACL은 포트와 프로토콜(TCP/UDP) 단위로 트래픽을 허용/차단하므로, 포트와 TCP/UDP 개념이 이들을 이해하는 출발점이다. [[VPC]]

## 면접 체크포인트

- L3(호스트까지)와 L4(포트로 애플리케이션까지)의 경계, 멀티플렉싱
- 세그먼트가 패킷 안에 캡슐화되는 계층 구조
- 포트 범위(0~65535), 웰노운 vs 임시 포트, 서버가 고정 포트를 쓰는 이유
- TCP의 신뢰성 메커니즘(확인/재전송/순서)과 그 비용(지연)
- TCP 세그먼트 핵심 필드(seq, ack, window, flags)와 흐름 제어
- UDP가 단순/빠른 이유와 적합 사례, TCP/UDP 선택 기준
- NLB(L4) vs ALB(L7)의 동작 계층 차이, SG/NACL이 포트/프로토콜 기준인 점

## 출처

- [OSI 7 Layer 기초: Transport Layer (TCP, UDP, 포트, 세그먼트) — YouTube](https://www.youtube.com/watch?v=mHwLHubS_iM&list=PLfth0bK2MgIYuFahPhXTpTomkwVx5Fl-v&index=3)

## 관련 문서

- [[Network-Layer|네트워크 계층 (L3, IP, 라우팅, ARP)]]
- [[TCP-Handshake|TCP Handshake (3-way/4-way, TIME_WAIT, RTT 비용)]]
- [[OSI-7-Layer|OSI 7계층 전체 지도]]
- [[ELB|AWS ELB (NLB L4 vs ALB L7)]]
- [[Browser-URL-Flow|브라우저 URL 입력 흐름]]
- [[네트워크(Network)|카테고리 인덱스]]
