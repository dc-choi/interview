---
tags: [architecture, modular-monolith, monolith, msa, module]
status: done
category: "아키텍처&설계(Architecture&Design)"
aliases: ["Modular Monolith", "모듈러 모노리스"]
---

# Modular Monolith

단일 배포 단위 안에서 도메인별로 강한 경계를 가진 모듈들로 시스템을 구성하는 아키텍처. 모노리스의 단순한 운영과 마이크로서비스의 독립성 사이의 현실적인 중간 지점이다. 좋은 아키텍처의 기준은 멋진 구조가 아니라 변경을 빠르고 안전하게 수용해 비즈니스 가치를 전달하는 것(→ [[Why-Architecture-Matters]])이며, 모듈러 모노리스는 분산 시스템의 복잡도 없이 그 경계를 미리 연습하게 해준다.

## 왜 나오는가 — 성급한 MSA의 역설

마이크로서비스가 항상 정답은 아니다. 조직 규모, 운영 역량, 시스템 복잡도에 안 맞으면 오히려 복잡도와 운영 부담을 키운다. MSA에서 다시 모노리스(모듈러)로 합치는 선택이 합리적일 수 있는데, MSA가 나빠서가 아니라 상황에 안 맞아서다.

- **소수 인원 다수 서비스 → 경계 붕괴**: 개발자가 여러 서비스를 동시에 맡으면 경계를 지키기보다 일단 동작하게 만드는 선택이 반복되고 기술 부채가 분산된 채 쌓인다.
- **공유 데이터 모델은 숨은 폭탄**: 서비스는 나뉘었는데 여러 서비스가 같은 테이블, 같은 모델을 직접 수정하면 진짜 독립성이 없다. 한쪽 변경이 다른 쪽 장애로 번지는 분산된 스파게티가 된다. MSA의 전제는 서비스별 자기 DB 소유다 (→ [[Monolith-vs-Microservice]] Fowler 특성 6번).
- **서비스 분리는 커뮤니케이션 비용을 만든다**: 여러 서비스가 함께 바뀌어야 하는 요구가 잦은데 팀이 다르면 배포 일정, API 변경, 테스트 범위, 장애 책임을 모두 조율해야 한다. 한 팀 안 대화로 끝날 일이 팀 간 일정 조율 문제가 된다. 서비스를 나누는 것은 코드만이 아니라 소통 비용을 나누는 것이다.

## 구조

- 배포는 하나, 내부는 도메인별 모듈(예: 상품, 주문, 배송). 각 모듈은 자기 책임을 갖고 외부에는 공개 인터페이스만 노출하며 내부 구현은 숨긴다.
- 모듈 간 협력은 공개 API를 통해서만. 다른 모듈의 내부 클래스나 테이블을 직접 참조하지 않는다.
- 원칙은 응집도는 높이고 결합도는 낮추는 것 — 같이 바뀌는 것은 한 모듈에, 따로 바뀌는 것은 다른 모듈에.

## 도메인 기준 모듈 (레이어 기준 아님)

controller, service, repository 같은 기술 계층으로만 나누면 기능 하나를 바꿀 때 모든 계층을 건드린다. 상품, 주문, 배송처럼 비즈니스 기능으로 나누면 변경 범위가 명확해지고, 각 모듈이 스스로 동작하고 스스로 테스트 가능하며, 나중에 독립 서비스로 떼기 쉽다. 같은 원리가 패키지 구조에도 적용된다 (→ [[Elegant-OOP-Design|레이어 중심 vs 도메인 중심 패키지]]).

## 실행 단위와 데이터 경계 — 같은 릴리스, 다른 런타임

하나의 코드베이스와 릴리스 안에서도 실행 단위는 목적별로 나눌 수 있다. 웹 요청을 받는 api, 이벤트를 처리하는 worker, 스케줄 작업의 batch, 내부 운영 도구 console을 같은 저장소에서 만들어 하나의 릴리스로 배포하되, 런타임에서는 역할별 컨테이너로 나눠 실행한다.

- 분리의 동기는 성능만이 아니다. 사용자 요청, 후속 작업, 정기 작업, 운영자 도구는 **실패 방식과 권한 모델과 관측 기준이 다르다.** api는 응답 시간을, worker는 처리량과 적체를 중심으로 운영하고 자원과 확장 기준을 따로 둔다.
- console 분리는 보호 장치다 — 운영자의 실수가 사용자 트래픽을 받는 프로세스에 닿지 않게 하고 권한과 감사 기록을 명확히 남긴다. worker는 도메인별로 큐와 소비 경로를 나눠 한 큐의 지연이 다른 큐를 막지 않게 한다.
- 릴리스 경계는 하나이므로 작은 변경도 모든 실행 단위에 미치는 영향을 함께 검증한다. 실행의 격리를 얻는 대신 하나의 릴리스가 갖는 결합을 받아들인 트레이드오프다.
- 데이터도 같은 원리다. 물리 데이터베이스를 공유하더라도 논리적 소유 경계를 나누고, 다른 모듈은 테이블을 직접 읽지 않고 소유 모듈이 제공하는 인터페이스로만 접근한다. 캐시는 원본을 대신하지 않는 완충으로 두고, 검색 색인은 원본에서 파생된 조회 모델로 취급해 재색인으로 다시 만들 수 있게 한다.

## 경계는 의지가 아니라 도구로 강제한다

개발자가 늘 규칙을 지킬 것이라 기대하면 경계는 쉽게 무너진다. 컴파일러, 빌드 도구, 프레임워크의 힘을 빌린다.

- **접근 제어자**: Java의 public, protected, package-private로 공개할 것과 숨길 것을 구분한다. 모듈의 공개 API만 public, 내부 구현은 package-private으로 막는다.
- **빌드 도구**: Gradle 멀티 프로젝트로 모듈을 별도 프로젝트로 두고 의존성 방향을 명시적으로 통제한다. 허용되지 않은 의존은 컴파일에서 걸린다.
- **프레임워크**: Spring에서 모든 빈이 한 컨테이너에 있으면 내부 구현까지 서로 참조할 수 있다. 모듈별로 별도 ApplicationContext를 두고 공개할 빈만 부모 컨텍스트로 노출하면, 같은 애플리케이션 안에서도 모듈 경계가 강해진다.

핵심은 내부 구현은 숨기고 공개 API만 쓰게 만드는 것이다.

빌드와 CI에 연결하는 실행 장치까지 더하면 경계는 합의가 아니라 규칙이 된다.

- **아키텍처 테스트**: 모듈 사이의 의존 방향과 패키지 구조를 테스트로 검사한다(Spring Modulith, ArchUnit). 트랜잭션을 열 때 어느 데이터베이스로 향하는지 명시하도록 강제하고, 현재 시각과 난수처럼 실행마다 달라지는 값은 주입해 테스트 재현성을 확보한다.
- **기준선 동결**: 큰 코드베이스에 규칙을 한 번에 적용하려 하면 변화가 멈춘다. 이미 쌓인 위반은 기준선으로 동결하고 새로 들어오는 위반만 차단하며, 손대는 영역의 부채를 함께 줄인다. 사람의 합의는 흐려질 수 있지만 CI에서 실행되는 규칙은 매번 같은 기준으로 시스템을 붙잡는다.
- **계약을 도구 체인에 연결**: API 명세를 OpenAPI 계약으로 두고 PR로 검토하며, 병합되면 서버 스텁과 클라이언트 코드를 생성하고 하위 호환을 깨는 변경은 CI에서 걸러낸다. 계약이 문서에 머물면 부채가 되기 쉽고, 코드 생성과 CI에 연결되면 구조를 지키는 장치가 된다.
- 모든 원칙을 CI로 막지는 않는다. 인터페이스 이름이나 캐시 만료 시간처럼 설계 의도를 봐야 하는 규칙은 코드 작성 규칙과 리뷰로 지키고, 기계적으로 찾을 수 있는 위반만 CI가 차단한다.

## 헥사고날과의 관계

모듈 내부를 외부 기술로부터 보호하는 것은 [[DDD&Hexagonal|헥사고날 아키텍처]]의 원리와 같다. 도메인 로직을 중앙에, 데이터베이스, 메시징, 웹 API 같은 기술을 바깥에 두고 인터페이스로만 소통하면, 자주 바뀌는 바깥을 교체 가능한 부품으로 다룰 수 있어 DB나 외부 호출 방식을 바꿔도 핵심 로직은 덜 흔들린다. 각 모듈을 하나의 작은 헥사곤으로 구성한다고 보면 된다.

## 점진적 서비스 추출

모듈러 모노리스의 실익은 필요할 때 특정 모듈을 독립 서비스로 떼어낼 수 있다는 점이다. 경계가 이미 잡혀 있으면 바뀌는 것은 주로 통합 방식이다 — 내부 인터페이스 호출을 HTTP API 호출로 교체하는 정도. 처음부터 경계를 잘 잡아두면 단일 시스템과 분산 시스템을 양방향으로 오갈 여지가 생긴다. 레거시를 점진적으로 떼어내는 전략은 [[Legacy-Modernization-Strategies|Strangler Fig]]와 연결된다.

경계 조정은 나누는 방향만이 아니다. 네트워크 왕복과 외부 운영이 오히려 복잡성을 키우는 의존은 내부 모듈로 다시 가져오고, 한때 분리했던 경계를 재결합하기도 한다. 공통화도 마찬가지로 신중하게 한다 — 두 번째 사용처가 생겼다는 이유만으로 공통 모듈로 올리지 않고, 실제로 같은 이유로 변하는 규칙인지 확인한 뒤에 올린다. 목표는 항상 나누는 것이 아니라 필요할 때 나누고, 필요할 때 다시 붙이고, 역할이 끝난 경계는 제거하는 것이다.

## 콘웨이 법칙과 함께 설계한다

시스템 구조는 조직의 의사소통 구조를 닮는다(콘웨이 법칙). 독립 팀이 명확한 비즈니스 책임을 가지면 서비스 분할이 효과적이지만, 작은 팀이 많은 서비스를 억지로 운영하면 혼란만 커진다. 조직이 아직 하나의 팀이라면 모듈러 모노리스가 조직 구조에 더 잘 맞는다. 아키텍처는 조직 구조와 함께 설계해야 한다.

## 언제 무엇을

- **모듈러 모노리스**: 작은~중간 팀, 도메인 경계를 학습, 검증하는 중, 분산 운영 역량이 아직 없을 때. MSA 전환 판단을 미루면서도 경계를 미리 연습한다.
- **MSA로 승격**: 팀이 커져 독립 배포가 필요하고, 모듈별 부하나 기술 요구가 크게 다르며, 인프라 자동화와 관측이 준비됐을 때 (→ [[Monolith-vs-Microservice]]).
- 먼저 모노리스를 잘 다루지 못하면 MSA에서도 같은 문제가 분산되어 반복된다. 질문은 무엇이 더 멋진가가 아니라, 지금 우리 조직과 시스템이 변경을 가장 안전하고 빠르게 수용하는 방법은 무엇인가이다.

## 사례 — 천만 MAU를 지탱한 모듈형 모놀리스

- 지역성과 인증 정책이 서로 다른 4개 커뮤니티 서비스의 서버 기능을 하나의 서버앱에 두고 내부 모듈 경계로 관리한 사례 (당근 커뮤니티, 2021~2026). 약 100 RPS에서 시작해 약 20,000 RPS까지 같은 구조로 성장했고, 게시글, 댓글, 멤버십처럼 닮은 구성 요소가 서비스마다 반복되는 조건에서 위치 정보, 투표, 레퍼럴, 인증, 알림 같은 공통 부분을 컴포넌트로 뽑아 재사용했다.
- 선택 기준은 누가 이 구조를 계속 관리하고 운용할 수 있는가였다. 작은 팀이 만들기와 운영을 함께 감당해야 하는 조건에서는 구축, 테스트, 배포, 운영의 조율 비용이 낮은 쪽이 현실적이었다.
- 성격이 다른 워크로드(ML 모델 서빙 등)는 밖으로 분리하되, 분리를 문제 해결 자체로 보지는 않았다. 이해하지 못한 도메인 경계를 네트워크 너머로 옮겨도 책임은 선명해지지 않고, 잘못된 모델과 강한 결합을 그대로 둔 분리는 네트워크 실패와 데이터 일관성이라는 더 큰 복잡성을 만든다.
- 부하와 장애가 운영 장치를 하나씩 만들어간 과정은 [[Failure-Evolution-Under-Load|부하에 따른 장애 진화]] 참고.

## 면접 체크포인트

- 모듈러 모노리스가 모노리스, MSA와 다른 점 (단일 배포 + 강한 내부 경계)
- 공유 DB가 MSA의 독립성을 깨는 이유 (분산된 스파게티)
- 모듈 경계를 의지가 아니라 도구로 강제하는 방법 (접근 제어자, Gradle, ApplicationContext)
- 도메인 기준 모듈과 레이어 기준 모듈의 변경 범위 차이
- 모듈에서 서비스로 추출할 때 실제로 바뀌는 것 (통합 방식, 내부 호출 → HTTP)
- 성급한 MSA의 함정과 Monolith First
- 하나의 릴리스 안에서 실행 단위를 나누는 이유 (실패 방식, 권한, 확장 기준의 차이)와 그 트레이드오프 (릴리스 결합)
- 기준선 동결이 필요한 이유 (규칙을 일괄 적용하면 변화가 멈춘다)

## 출처

- [우아한테크 — 모듈러 모노리스와 마이크로서비스](https://www.youtube.com/watch?v=SrQeIz3gXZg)
- [천만 MAU를 지탱하는 커뮤니티 시스템을 소개해요 — 당근 기술 블로그](https://medium.com/daangn/%EC%B2%9C%EB%A7%8C-mau%EB%A5%BC-%EC%A7%80%ED%83%B1%ED%95%98%EB%8A%94-%EC%BB%A4%EB%AE%A4%EB%8B%88%ED%8B%B0-%EC%8B%9C%EC%8A%A4%ED%85%9C%EC%9D%84-%EC%86%8C%EA%B0%9C%ED%95%B4%EC%9A%94-090fb4021e20)

## 관련 문서

- [[Monolith-vs-Microservice|Monolith vs Microservice (Fowler 9특성, 언제 MSA)]]
- [[DDD&Hexagonal|DDD, Hexagonal (모듈 내부 보호)]]
- [[Elegant-OOP-Design|우아한 객체지향 (의존성, 도메인 패키지)]]
- [[Why-Architecture-Matters|아키텍처의 중요성 (변경 대응, 결정 비가역성)]]
- [[Monorepo-Architecture|모노레포 아키텍처]]
- [[Legacy-Modernization-Strategies|레거시 현대화 (Strangler Fig)]]
- [[Event-Driven-Architecture|Event-Driven Architecture (모듈 간 이벤트)]]
- [[Failure-Evolution-Under-Load|부하에 따른 장애 진화 (같은 시스템의 운영 축 — 재시도 증폭, 운영 리듬)]]
